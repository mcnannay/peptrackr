version: "3.9"
services:
  peptrackr:
    build:
      context: .
      dockerfile: Dockerfile
    image: peptrackr:v16.5.2-serverusers
    ports:
      - "8085:80"
    restart: unless-stopped
    volumes:
      - peptrackr_data:/data
volumes:
  peptrackr_data:
