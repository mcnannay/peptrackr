services:
  peptrackr:
    build: .
    container_name: peptrackr
    ports:
      - "8085:8080"   # host:container
    environment:
      - DATA_DIR=/data
    volumes:
      - peptrackr_data:/data
    restart: unless-stopped

volumes:
  peptrackr_data:
