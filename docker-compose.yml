version: "3.9"
services:
  peptrackr:
    build:
      context: .
      dockerfile: Dockerfile
    image: peptrackr:latest
    ports:
      - "8085:80"
    restart: unless-stopped
    volumes:
      - peptrackr_data:/data

volumes:
  peptrackr_data:
